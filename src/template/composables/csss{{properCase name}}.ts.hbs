import { useComponent } from '@/packages/composables/Comp';

/**
* @description use csss {{dashCase name}} composable
*/
export const useCsss{{properCase name}} = (props: UseCsss{{ properCase name }}Props) => {
  return {
    ...useComponent<C{{properCase name}}Api, UseCsss{{ properCase name }}Props>(props)
  };
};